{
  "name": "pykrx-mcp-dev",
  "version": "0.1.0",
  "private": true,
  "description": "Development dependencies for pykrx-mcp",
  "devDependencies": {
    "@modelcontextprotocol/inspector": "^0.1.0"
  },
  "scripts": {
    "inspector": "npx @modelcontextprotocol/inspector uv --directory . run --with-editable . python -m pykrx_mcp.server",
    "test": "pytest -v",
    "format": "black src/",
    "lint": "ruff check src/",
    "lint:fix": "ruff check --fix src/"
  }
}
